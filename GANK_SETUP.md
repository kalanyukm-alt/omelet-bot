# ตั้งค่ารับทิป Gank

ระบบยังใช้ปุ่มตัวละครและ Role เดิมทั้งหมด แต่เปลี่ยนจากซอง TrueMoney เป็นหน้า Tip ของ Gank

## ตัวแปรที่ต้องตั้งในเครื่องโฮสต์บอต

เพิ่มตัวแปรนี้ในหน้า Environment เดียวกับที่ตั้ง `TOKEN` และ `LOG_CHANNEL`:

```env
GANK_STREAM_URL=https://stream.ganknow.com/ใส่รหัส-Stream-Alert-ของคุณ
```

ต้องใช้ URL จากแท็บ **การแจ้งเตือนสตรีม** และ URL ต้องไม่มี `/qr-code` ต่อท้าย

หน้า Tip ตั้งค่าเริ่มต้นไว้เป็น `https://ganknow.com/omelettt1101/tip` แล้ว หากภายหลังเปลี่ยนชื่อบัญชีให้เพิ่ม:

```env
GANK_TIP_URL=https://ganknow.com/ชื่อบัญชีใหม่/tip
```

จากนั้น restart หรือ redeploy บอต ที่ console ต้องเห็นข้อความ:

```text
[GANK] เชื่อมต่อ Stream Alert แล้ว
```

## ทดสอบโดยไม่จ่ายเงินจริง

1. กดปุ่ม **ทดสอบการแจ้งเตือน** ในหน้า Gank หนึ่งครั้ง
2. บอตต้องไม่มอบ Role
3. ที่ console ควรเห็น `[GANK] ข้าม Test Alert`

หาก Test Alert ไม่ถูกระบุว่าเป็นรายการทดสอบ ให้หยุดรับเงินจริงก่อนและเก็บ payload ที่ตัดชื่อ/ข้อความส่วนตัวออกมาตรวจเพิ่ม

## ขั้นตอนของผู้ซื้อ

1. กดปุ่มตัวละครใน Discord
2. คัดลอกรหัส `OML-XXXXXX` ที่บอตให้
3. เปิดหน้าจ่ายเงิน กรอกยอดตามที่บอตแจ้ง
4. วางรหัสในช่อง **ข้อความของคุณ** ให้ตรงทุกตัว
5. ชำระเงินและรอรับ Role ทาง Discord

รายการมีอายุ 60 นาที ยอดที่จ่ายไม่ครบสามารถจ่ายเพิ่มด้วยรหัสเดิมได้ และ event เดิมจะไม่ถูกนับซ้ำ
